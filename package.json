{
  "name": "waffir",
  "private": true,
  "version": "1.0.0",
  "description": "وفّر — مساعد التسوق الذكي ومقارنة أسعار البقالة في السعودية",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=server & npm run dev --workspace=client & wait",
    "build": "npm run build --workspace=client && npm run build --workspace=server",
    "start": "npm run start --workspace=server",
    "seed": "npm run seed --workspace=server",
    "db:migrate": "npm run db:migrate --workspace=server",
    "test": "npm run test --workspace=server",
    "test:e2e": "playwright test",
    "lint": "npm run typecheck --workspace=server && npm run typecheck --workspace=client"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "drizzle-orm": "^0.45.2"
  },
  "engines": {
    "node": ">=20"
  }
}
