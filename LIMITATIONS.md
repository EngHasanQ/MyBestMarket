# حدود صادقة — LIMITATIONS

هذا الملف يوثّق بصراحة ما يعمل فعلياً، وما هو محاكاة، وما يلزم لتشغيل التطبيق بدقة حقيقية. (مطلوب بموجب القسم 12 من المواصفة.)

## المحوّلات (الكشط) — الواقع الحالي

| المتجر | الحالة | السبب |
|---|---|---|
| بنده | **fixtures** (بيانات بشكل الاستجابة الحقيقي) | الموقع خلف حماية ضد الروبوتات، وشروط الاستخدام لا تجيز الكشط الآلي صراحة. الاستبدال بكشط حي يتطلب مراجعة قانونية/تجارية أو اتفاق بيانات |
| العثيم، الدانوب، كارفور، التميمي | **لا محوّل** — أسعارها من البذر ومن تقارير المستخدمين والمجلات | نفس قيود الحماية والشروط |
| متاجر منصة سلة المكتشفة | **محوّل حي عام كامل الكتالوج** (`sallaStorefront.ts`): يزحف شجرة التصنيفات ثم كل صفحات كل تصنيف، مع الصور وروابط المنتجات | يعمل فقط بعد اكتشاف المتجر وتأكيد الأدمن للمصدر |
| متاجر منصة زد المكتشفة | **محوّل حي عام** (`zidStorefront.ts`) يجرّب أنماط نقاط زد الشائعة ويعتمد أول نمط يستجيب | قوالب زد تختلف؛ بعض المتاجر قد تحتاج ضبط endpoint يدوياً من ملف المصدر |

**الخلاصة:** الكشط الحي الموثوق للسلاسل الكبرى غير مضمون تقنياً وقانونياً. التصميم يعوّض ذلك بطبقات: المجلات الأسبوعية (مصدر رسمي منشور) + فواتير المستخدمين (حقيقة أرضية) + التقارير اليدوية.

## إثبات السعر (سبرنت v2)

- كل سعر مستورد يحمل دليلاً في `price_evidence`: صورة المنتج تُنزَّل من نفس مصدر السعر (بهاش محتوى يمنع التكرار)، وصفحة المجلة عند اعتماد العروض، وصورة الفاتورة للأسعار الموثقة منها — تُعرض للمستخدم عبر زر "إثبات السعر" بمصغرات sharp.
- **حد حالي:** دليل المجلة هو صورة الصفحة كاملة لا قصاصة المنتج وحده (OCR النصي الحالي لا يحفظ إحداثيات)، ولا تُلتقط لقطات Playwright لبطاقات المنتجات لأن الكشط الحي للسلاسل الكبرى معطّل أصلاً (انظر أعلاه). السعر المدخل يدوياً من الأدمن يظهر بـ"لا يوجد إثبات مرفق".
- البيانات المبذورة (183 منتجاً التجريبية) بلا صور لأنها ليست من مصدر حي — الصور تتدفق تلقائياً فور تأكيد أول مصدر سلة/زد حقيقي أو رفع فواتير/مجلات، وتظهر البطاقات قبلها بأيقونة التصنيف (بديل SVG محايد).

## اكتمال الكتالوج

- إجابة "البحث عن زبادي لا يجد شيئاً": البحث الآن يتوسع بالمرادفات (زبادي=روب=لبن رائب…) من جدول `search_synonyms` القابل للإثراء، والمنتجات الخام غير المطابقة من أي مصدر تُنشأ تلقائياً (`auto_created`) بدل إسقاطها، مع أداة دمج للأدمن.
- تقرير التغطية في لوحة الأدمن يعرض الأرقام الحقيقية لكل متجر (منتجات بسعر، أسماء، منشأة تلقائياً، تصنيفات متجر غير مربوطة) — الأرقام الحية تتطلب مصدر سلة/زد مؤكداً أو تدفق مجلات/فواتير؛ بيئة المستودع لا تضم اتفاق بيانات مع السلاسل الكبرى.
- فحص Lighthouse (هدف ≥90) لم يُشغَّل في بيئة CI هذه — الصور كسولة التحميل وبمصغرات والتطبيق مقسوم حِزماً، لكن القياس الفعلي يلزم تشغيله على النشر.

## دقة الأسعار — ماذا تعني "99%" عملياً

- النسبة المعلنة تقاس بمؤشر KPI محدد: **% الأسعار المعروضة بثقة ≥90 وعمر ≤7 أيام** (لوحة الإدارة ← الجودة، ولقطة يومية في `kpi_snapshots`).
- بلوغ النسبة والحفاظ عليها يتطلب تدفقاً مستمراً من الفواتير والمجلات المعتمدة — ليس وعداً تلقائياً من الكشط.
- أسعار المتاجر الإلكترونية تُعرض بوسم "تقديري" بعد معايرتها بمعامل السلسلة، وقد تختلف عن الرف فعلياً حتى تتراكم عينات معايرة كافية (نافذة 7 أيام، ~20 عينة).

## OCR

- tesseract.js (ara+eng) يعمل فعلياً على الصور المرفوعة، لكن دقة العربية على صور الجوال منخفضة الجودة متواضعة — لذلك **كل مستخرجات المجلات تمر بقائمة مراجعة بشرية إلزامية** قبل النشر، وبنود الفواتير غير الواثقة تُعاد للمستخدم لتأكيدها.
- أول استدعاء OCR بعد إقلاع الخادم يحمّل ملفات traineddata (~15م.ب) — تأخير لمرة واحدة.
- اختبارات OCR الآلية تستخدم نصوص fixture (لا صور) لثبات النتائج؛ مسار الصورة الكامل مغطى يدوياً.

## الاكتشاف (Google Places)

- بدون `GOOGLE_MAPS_API_KEY` يعمل الاكتشاف على fixtures فقط (كما في الاختبارات). المفتاح مدفوع: الاكتشاف مصمم شهرياً لكل مدينة فيها مستخدمون، ولا يُستدعى أبداً لكل طلب مستخدم.
- تأهيل المصادر يفحص الصفحة الرئيسة فقط (توقيعات سلة/زد/شوبيفاي + روابط عروض) — المواقع المخصصة المعقدة قد تحتاج إدخال المصدر يدوياً من لوحة الإدارة.

## ZATCA QR

- يُفك ترميز TLV الأساسي (البائع، الرقم الضريبي، الوقت، الإجمالي) للتحقق من هوية المتجر. **لا يتحقق من التوقيع التشفيري** للمرحلة الثانية من فوترة — كافٍ كمضاد تلاعب أولي، غير كافٍ كإثبات قانوني.

## ما يجب على الأدمن فعله أسبوعياً للبقاء عند المستوى المستهدف

1. **رفع مجلات العروض** لكل سلسلة (الأربعاء/الخميس غالباً — مراقب المجلات ينبهك عند رصد تغيير صفحة العروض).
2. **تصفية قائمة المراجعة يومياً** — البنود المعلقة >24 ساعة تظهر تحذيراً في لوحة الجودة.
3. **مراقبة تبويب الوظائف** — مصدر فاشل 3 مرات متتالية يُعلَّم تلقائياً وتقادم أسعاره، ويصلك إشعار.
4. **تأكيد المصادر المكتشفة حديثاً** (مرة واحدة لكل سلسلة) قبل ظهور فروعها للمستخدمين.

## حدود تقنية أخرى

- الإشعارات داخل التطبيق فقط (صفحة + نقطة حمراء) — لا Web Push/FCM بعد.
- المزامنة دون اتصال تغطي وضع التسوق (شطب/سعر/إنهاء) — التصفح والبحث يتطلبان اتصالاً.
- توليد القائمة الشهرية يتطلب ≥2 شراء للمنتج (أو شراء واحداً عمره ≥21 يوماً) — مستخدم جديد بلا تاريخ يبدأ يدوياً.
- `node-cron` داخل عملية الخادم: مناسب لخدمة Railway واحدة؛ التوسع لعدة نسخ يتطلب نقل الجدولة لعامل مستقل أو قفل قاعدة بيانات.
- اختبارات E2E تعمل على Chromium بعرض 390×844 (iPhone 12 يتطلب WebKit وهو غير مثبت في بيئة CI الحالية).
